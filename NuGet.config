<!-- Local NuGet configuration that uses a local NuGet repository to easily
     reference our native library. -->
<configuration>
    <packageSources>
        <add key="tomat.fnb.local" value="./nuget/" />
    </packageSources>
</configuration>